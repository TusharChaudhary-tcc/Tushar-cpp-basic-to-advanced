#include <bits/stdc++.h>
using namespace std;
// Note:- use break always in a if-else condition when it is in a loop-for, while, do-while
//        use return 0 if a if-else condition without loop

// break:
// - Used to exit the nearest loop (for, while, do-while) or a switch statement.
// - Cannot be used outside a loop or switch.

// return:
// - Used to exit the current function immediately.
// - In main(), 'return 0;' ends the program.


int main(){
    // Prime Number:- exactly two factors  1 and the number itself
    
    int num;
    cin>>num;

    // if(num < 2){
    // cout << "Not a Prime Number";
    // return 0;
    // }
    // bool isPrime=true;
    // for(int i=2; i*i<=num; i++){
    //     if(num%i==0){
    //         isPrime=false;
    //     }

    // }
    // if(isPrime) cout<<"Prime Number"<<endl;
    // else cout<<"Not a Prime Number"<<endl;




}